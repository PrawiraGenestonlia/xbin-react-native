import { StyleSheet } from 'react-native';

const style =
  StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
    },
    text: {
      marginVertical: 16,
    },
    intro: {
      marginTop: 80,
    },
    p: {
      marginTop: 10,
      fontSize: 20,
      textAlign: 'center',
    }
  });

export default style;
